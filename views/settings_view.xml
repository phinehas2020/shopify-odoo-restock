<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Standalone settings form for our Shopify Restock configuration -->
  <record id="view_shopify_restock_settings" model="ir.ui.view">
    <field name="name">shopify.restock.settings.form</field>
    <field name="model">res.config.settings</field>
    <field name="arch" type="xml">
      <form string="Shopify Restock Settings">
        <group>
          <group string="Shopify API">
            <field name="shopify_store_domain"/>
            <field name="shopify_access_token" password="True"/>
            <field name="shopify_api_version"/>
          </group>
          <group string="Inventory Location">
            <field name="shopify_location_id_global"/>
            <field name="shopify_location_id_numeric"/>
          </group>
          <group string="To-do">
            <field name="restock_project_id"/>
          </group>
          <group string="Odoo Inventory">
            <field name="restock_odoo_location_id"/>
          </group>
          <group string="Webhook">
            <field name="restock_webhook_enabled"/>
            <field name="restock_webhook_url"/>
          </group>
        </group>
        <footer>
          <button string="Save" type="object" name="execute" class="btn-primary"/>
          <button string="Close" class="btn-secondary" special="cancel"/>
        </footer>
      </form>
    </field>
  </record>

  <record id="action_shopify_restock_settings" model="ir.actions.act_window">
    <field name="name">Shopify Restock Settings</field>
    <field name="res_model">res.config.settings</field>
    <field name="view_mode">form</field>
    <field name="view_id" ref="view_shopify_restock_settings"/>
    <field name="target">new</field>
    <field name="context">{"default_shopify_api_version": "2023-04"}</field>
  </record>
</odoo>
